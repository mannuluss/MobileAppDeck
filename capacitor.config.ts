import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mannulus.MobileAppDeck',
  appName: 'MobileAppDeck',
  webDir: 'www',
  server: {
    androidScheme: 'https'//todo revisar
  }
};

export default config;
