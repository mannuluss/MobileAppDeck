import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'MobileAppDeck',
  webDir: 'www',
  server: {
    androidScheme: 'https'//todo revisar
  }
};

export default config;
